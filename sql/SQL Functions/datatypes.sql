BINARY

BOOLEAN

DATE

FLOAT

INTEGER

NUMBER

TIMESTAMP (TIMESTAMP_LTZ (values with local time zone), TIMESTAMP_NTZ (values with no time zone), TIMESTAMP_TZ (values with time zone))

VARCHAR

VARIANT